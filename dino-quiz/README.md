# Dino Quiz

Open `index.html` directly to play with browser-only score saving.

For permanent leaderboard saving, start the local server from the repo root:

```bash
node dino-quiz/server.js
```

Then open the printed `localhost` address. The **Save Leaderboard** button writes to `data/leaderboard.json`, which can be committed and pushed with the rest of the repo.
